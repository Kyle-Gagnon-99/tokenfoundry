//! The `config` module defines the configuration structures for the token-shift library, which can be used to specify various
//! options and settings for token parsing, transformation, and generation. This includes options for input and output formats,
//! token naming conventions, reference resolution, and more. The configuration structures can be used to customize
//! the behavior of the token-shift library to fit different use cases and requirements.

#[derive(Debug, Clone, serde::Serialize)]
pub struct Config {}
